---
date: 2026-06-22
run_started_utc: "09:00:00"
sources_scanned: 6
sources_unreachable: 1
items_considered: 5
items_opened_pr: 1
items_deferred: 0
items_out_of_scope: 4
prs_opened: [69]
---

# 2026-06-22 — Daily improvement routine

## Summary

Claude Code v2.1.186 (2026-06-22) fixed a bug where `Agent(type)` deny rules were not enforced for named subagent spawns. The CSIS analogue was present: `run_iteration()` only had an explicit `enforce()` call at the Builder site — Verifier (T1), Researcher, Librarian, and Auditor all delegated without a tier gate. PR #69 closes that gap by adding `Coordinator._enforce_agent()` and wiring it before every agent invocation. Four other in-window items were out-of-scope; three of them were also covered in the June 20 run (PR #68).

## Items considered

### opened-PR — Agent(type) deny rules not enforced for named subagent spawns (Claude Code v2.1.186)
- URL: https://code.claude.com/docs/en/changelog
- Source: code.claude.com/docs/en/changelog, published 2026-06-22 (v2.1.186)
- Theme: 6 — Substrate / capability boundaries (also Theme 2: trust + verification)
- Touches: `csis/agents/coordinator.py` (`run_iteration`, new `_enforce_agent` helper), `tests/test_safety.py`
- Size: small (141 LOC added including tests)
- Risk: low (additive; all current agents are T0/T1 and pass their gates; no chokepoint internals touched)
- PR: #69
- Chokepoint argument: The single chokepoint is `run_iteration()` in `coordinator.py`. The five new `_enforce_agent()` calls are purely additive — each runs before an existing agent invocation and emits a `coordinator.tier.violation` event + rollback on failure, exactly mirroring the Builder's prior pattern. Cycle-9 protected chokepoints (`Coordinator.__init__`, `_BackendTracker`, `writer_iteration_id`, promotion CAS) are untouched. The Verifier case is load-bearing: Verifier ceiling is T1 in `TierGuard` but had zero enforcement — a future PR bumping it to T2 would have silently bypassed `PHASE_0_CEILING` with no event-log record.

### out-of-scope — Claude Code v2.1.185 stream-stall hint timing change (2026-06-20)
- URL: https://code.claude.com/docs/en/changelog
- UX change: stream-stall hint delay increased from 10s to 20s. No CSIS theme mapping.

### out-of-scope — Anthropic opens Seoul office (2026-06-17)
- URL: https://www.anthropic.com/news/seoul-office-partnerships-korean-ai-ecosystem
- Business/partnership announcement. Also covered in June 20 run (PR #68).

### out-of-scope — Project Fetch: Phase two (2026-06-18)
- URL: https://www.anthropic.com/research/project-fetch-phase-two
- Robotics research evaluating Claude on physical quadruped control. No CSIS module covers robotic control. Also covered in June 20 run (PR #68).

### out-of-scope — Agentic coding and persistent returns to expertise (2026-06-16)
- URL: https://www.anthropic.com/research/claude-code-expertise
- Economic study of user-AI collaboration in coding sessions. No CSIS code change implied. Also covered in June 20 run (PR #68).

## Reconsidered from prior runs

None.

## Sources unreachable

- `https://platform.claude.com/docs/en` — HTTP 404 on the `/en` path
- `https://platform.claude.com/docs` (redirect target for `docs.claude.com`) — returned `<HomePage />` React stub; no parseable documentation content
