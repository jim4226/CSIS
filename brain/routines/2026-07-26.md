---
date: 2026-07-26
run_started_utc: (see session start; UTC per environment clock)
sources_scanned: 6
sources_unreachable: 0
items_considered: 9
items_opened_pr: 1
items_deferred: 2
items_out_of_scope: 6
prs_opened: [119]
---

# 2026-07-26 — Daily improvement routine

## Summary

First real run of the routine (the ledger previously held only its initialization placeholder). Nine in-window items were considered across the six sources; one shipped as a docs-only PR, two are deferred with design sketches because they touch cost/pricing code or capability-tier enforcement, and six are out of scope.

## Items considered

### opened-PR — Claude Managed Agents memory_store/environment webhooks
- URL: https://platform.claude.com/docs/en/release-notes/overview (July 22, 2026 entry)
- Source: Claude Platform release notes, published 2026-07-22
- Theme: 4 — Persistent memory + context
- Touches: `ROADMAP.md` (new P1.9 entry)
- Size: trivial
- Risk: low
- PR: #119

### deferred — Claude Opus 5 launch vs. stale checkpoint model strings in CSIS
- URL: https://www.anthropic.com/news/claude-opus-5 (and the July 24, 2026 platform release-notes entry)
- Source: Anthropic News + Claude Platform release notes, published 2026-07-24
- Theme: 2 — Trust + verification (the builder/verifier cross-checkpoint identity is literally instantiated by the two model-name constants)
- Touches: `csis/backends/anthropic.py` (`_DEFAULT_MODEL_MAP`, currently `claude-opus-4-7` / `claude-sonnet-4-6`), `csis/budget.py` (per-model pricing table)
- Size: medium
- Risk: medium
- Design sketch: a `grep` for the two model strings turns up 21 files, including `brain/critiques/` and `brain/plans/` — the historical audit trail, which must **not** be rewritten (those docs describe what was true when each cycle ran). The real change is narrower: bump `_DEFAULT_MODEL_MAP` in `csis/backends/anthropic.py` and the corresponding per-MTok rates in `csis/budget.py` to the current generation, update only the *living* docs (`README.md`, `RUN.md`, `CSIS-architecture.html`, `docs/architecture.html`), and leave `brain/critiques/`, `brain/plans/`, and `brain/research/` untouched. Needs a regression test pinning the default model map and a second asserting the pricing table matches the documented per-MTok rates, since `BudgetTracker` cost math sits next to (though isn't itself) a cycle-9 chokepoint.
- Why deferred: touches cost/pricing code, not just a doc; conflating "historical record" files with "live config" files is exactly the kind of mistake the cycle log warns about, so this needs a scoped PR of its own rather than a same-day drive-by edit.

### deferred — Mid-conversation tool changes (beta): swap tools between turns, keep the prompt cache
- URL: https://platform.claude.com/docs/en/release-notes/overview (July 24, 2026 entry)
- Source: Claude Platform release notes, published 2026-07-24
- Theme: 6 — Substrate / capability boundaries
- Touches: `csis/domains/__init__.py` (adapter registry / protocol), `csis/substrate/` (capability tags)
- Size: medium
- Risk: medium
- Design sketch: today each domain adapter's tool set is fixed for the run. This beta feature would let a domain adapter add/remove tools mid-loop (e.g., a PR-maintenance adapter enabling a linter tool only once static analysis flags something) without blowing the prompt cache. But any tool added mid-conversation is a new capability surface introduced after the initial T0/T1 tier check — it would need its own tripwire/tier-guard pass before use, not just before the iteration starts.
- Why deferred: this is a new mutation point next to the capability-tier ceiling (Phase-0's T0/T1 enforcement). CONTRIBUTING.md is explicit that changes touching safety properties need a cycle-style critique + fix loop, not a same-day PR.

### out-of-scope — Introducing Claude Opus 5
- One line: covered under the deferred model-staleness item above; the launch announcement itself doesn't need a separate entry.

### out-of-scope — A research agenda for the Economic Futures Research Fund
- One line: philanthropic/economic-policy research priorities; no CSIS theme mapping.

### out-of-scope — Ask Claude about the Anthropic Economic Index
- One line: a consumer-facing connector for economic-index Q&A; not a technique CSIS's substrate or verification stack would consume.

### out-of-scope — Anthropic is donating another $20 million to Public First Action
- One line: philanthropy announcement; no CSIS theme mapping.

### out-of-scope — Project Pilot: Can AI control a drone?
- One line: Frontier Red Team capability research into UAV control — a threat-model/capability question, not a red-teaming *methodology* CSIS's curiosity/frontier-item generator could adopt.

### out-of-scope — Managed Agents session/API ergonomics (effort config, seeded session events, optional version field, thread-level event deltas)
- One line: four related July 22, 2026 release-notes items improving the Managed Agents API surface's ergonomics; CSIS's Phase-0 doesn't call the Managed Agents API today, and none of the four map to a specific one of the seven themes beyond general API polish.

## Reconsidered from prior runs
(empty — this is the first run with real entries; the ledger held only its initialization placeholder)

## Sources unreachable
(none — all six sources were reachable; two of the six required following a redirect: `docs.claude.com` → `platform.claude.com/docs`, and `platform.claude.com/docs/en` 404s so the release-notes page was fetched directly at `platform.claude.com/docs/en/release-notes/overview`)
