---
date: 2026-05-26
run_started_utc: 23:09:00
sources_scanned: 6
sources_unreachable: 1
items_considered: 4
items_opened_pr: 2
items_deferred: 2
items_out_of_scope: 0
prs_opened: [15, 16]
---

# 2026-05-26 — Daily improvement routine

## Summary

Two PRs opened: `external-content-layer` (#15) formalises Layer 3 of Anthropic's three-layer containment model in `csis/safety/`, and `domain-system-prompt` (#16) adds `system_prompt()` / `allowed_tools()` hook points to the Domain ABC, aligning the protocol with Anthropic's Agent SDK `AgentDefinition` pattern. The Stainless acquisition and Managed Agents dreaming feature remain deferred pending stable API surfaces.

## Items considered

### opened-PR — How we contain Claude across products
- URL: https://www.anthropic.com/engineering/how-we-contain-claude
- Source: Anthropic Engineering, published 2026-05-25
- Theme: 3 (Constitutional / safety primitives) + 6 (Substrate / capability boundaries)
- Touches: `csis/safety/external_content.py` (new), `tests/test_safety.py`
- Size: small (≤200 LOC)
- Risk: low
- PR: #15
- Summary: The article identifies three containment layers. CSIS had named modules for Layers 1 (capability tiers) and 2 (tripwires + constitution) but no Layer 3. `ExternalContentScanner` wraps `scan_text_no_history` so incoming tool results can be scanned as adversarial without polluting the operator-visible fired-history that tracks agent-produced outputs. F11 preserved: the Coordinator rolls back and logs; the agent sees only the rollback outcome.

### opened-PR — Agent SDK: AgentDefinition prompt + tools fields
- URL: https://code.claude.com/docs/en/agent-sdk/overview
- Source: Claude Code docs (June 15 credit-change notice confirms recent update), scanned 2026-05-26
- Theme: 1 (Multi-agent coordination) + 6 (Substrate / capability boundaries)
- Touches: `csis/domains/base.py`, `tests/test_domains.py`
- Size: trivial (≤50 LOC)
- Risk: low
- PR: #16
- Summary: `AgentDefinition(description, prompt, tools)` in the SDK establishes that instructions and tool access are part of an agent's definition, not runtime policy. CSIS's Domain ABC already had `describe()` (maps to `description`) but not `prompt` or `tools`. Added `system_prompt() -> str | None` and `allowed_tools() -> list[str] | None` with `None` defaults. The Phase-0 behaviour is unchanged; `base.py` already contained the note "future cycles can let the domain supply a domain-specific system prompt." Coordinator wiring is a Phase-1 step (P1.7).

### deferred — Claude Managed Agents: dreaming in research preview
- URL: https://platform.claude.com/docs/en/managed-agents/overview
- Source: Claude Platform docs, scanned 2026-05-26
- Theme: 4 (Persistent memory + context) + 1 (Multi-agent coordination)
- Touches: `csis/dreams/pipeline.py`, `csis/dreams/quality.py`, `pyproject.toml`
- Size: medium (≤500 LOC)
- Risk: medium (touches dreams pipeline which feeds into the verification/promotion flow; new external dependency)
- Why deferred: ROADMAP P1.1 explicitly targets the Dreams API once "accessible to the operator." The docs confirm `dreaming` is now in research preview but requires a separate access request (`https://claude.com/form/claude-managed-agents`). The correct gate is operator access confirmation, not a code PR. Design sketch: replace `MockDreamsBackend` in `csis/dreams/pipeline.py` with a `ManagedAgentsDreamsBackend` that calls the `POST /v1/agents/{agent_id}/sessions` endpoint, serialises `DreamFragment` objects from the event stream SSE, and feeds them into the existing `QualityScorer`. The biggest risk is session statefulness: Managed Agents sessions are durable; the mock discards state on process exit. A `session_id` store (analogous to `event_log_path` in `CSISConfig`) would be the new chokepoint.

### reconsidered — Anthropic acquires Stainless (still deferred)
- URL: https://www.anthropic.com/news/anthropic-acquires-stainless
- Source: First seen 2026-05-23, deferred; reconsidered again today
- Theme: 6 (Substrate / capability boundaries)
- Reconsidered note: Post-acquisition, Stainless's MCP server generation tooling has not yet shipped a stable public API surface CSIS can build against. The acquisition confirms Anthropic's direction (MCP-first connector ecosystem) but no actionable primitive has shipped. Status unchanged: deferred. Will reconsider once a `stainless generate-mcp` CLI or equivalent API is documented.

## Reconsidered from prior runs

**Stainless acquisition** — reconsidered as above; still deferred (no stable MCP generation API surface shipped post-acquisition).

## Sources unreachable

- `https://platform.claude.com/docs/en` — SPA returned a loading/Not Found state on the index page (same behaviour as May 24 and May 25 runs). Individual subpages under `/docs/en/managed-agents/` were reachable via direct URL. Logged as partial source failure; scan continued.

## Index backfill note

`brain/routines/index.md` on `main` is stale because log PRs #9, #11, and #14 are still open drafts (not merged). This run appends today's 3 new URL rows plus backfills the 8 confirmed prior-run rows reconstructed from reading the PR descriptions directly. Operator should merge log PRs in merge-date order (#9 → #11 → #14 → this PR) to keep the index current.
